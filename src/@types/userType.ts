export interface UserType {
    id: string,
    name: string,
    password: string
    photo: string,
    email: string,
    joined: Date,
    description: string,
    contact: number,
    status: boolean 
}